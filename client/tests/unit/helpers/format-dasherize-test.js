import { formatDasherize } from '../../../helpers/format-dasherize';
import { module, test } from 'qunit';

module('Unit | Helper | format dasherize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDasherize(42);
  assert.ok(result);
});
